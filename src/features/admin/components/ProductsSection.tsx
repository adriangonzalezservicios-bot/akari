import { useMemo, useState } from 'react';
import type { Product } from '../../../types';
export function ProductsSection({ products, onEdit, onDelete }: { products: Product[]; onEdit: (product: Partial<Product>) => void; onDelete: (id: string) => void }) {
 const [search, setSearch] = useState(''); const rows = useMemo(() => products.filter((p) => `${p.name} ${p.category}`.toLowerCase().includes(search.toLowerCase())), [products, search]);
 return <div className="space-y-4"><div className="flex gap-3"><input className="border rounded-lg px-3 py-2 flex-1" value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Buscar por nombre o categoría" /><button className="bg-akari-orange text-white rounded-lg px-4" onClick={() => onEdit({ category: 'Cocina', inventory: 10, price: 0, featured: false })}>Nuevo producto</button></div><div className="bg-white border rounded-2xl overflow-x-auto"><table className="w-full text-left"><thead><tr className="border-b"><th className="p-3">Producto</th><th>Precio</th><th>Stock</th><th>Categoría</th><th /></tr></thead><tbody>{rows.map((product) => <tr className="border-b" key={product.id}><td className="p-3 flex gap-3 items-center"><img className="w-10 h-10 object-cover rounded" src={product.image} alt="" />{product.name}</td><td>${product.price.toFixed(2)}</td><td>{product.inventory}</td><td>{product.category}</td><td><button className="mr-3 text-blue-600" onClick={() => onEdit(product)}>Editar</button><button className="text-red-600" onClick={() => onDelete(product.id)}>Eliminar</button></td></tr>)}</tbody></table></div></div>;
}
